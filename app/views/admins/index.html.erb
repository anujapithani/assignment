<% if current_admin %>
<div id="wrapper">
    <%= render "mains/navbar" %>
    <%= render "mains/sidebar" %>
    <%= render "mains/content" %>
</div>
<#%= link_to 'Logout' , '/logout' , :data=> { :confirm => 'Are you sure?' }%>
    <% else %>
    <%= link_to 'Signup', '/signup'%>
    <%= link_to 'Login', '/login'%>

    <% end %>